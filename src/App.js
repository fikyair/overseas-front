import React, {
    Component,
} from 'react';
import Router from './router/Router';
import './App.less';

class App extends Component {
    render() {
        return (<Router />
        );
    }
}

export default App;
